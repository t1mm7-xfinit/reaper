services:
  reaper:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - ENV=docker
      - HOST=0.0.0.0
      - PORT=8000
      - PROXY_PORT=8080
      - URL=https://api.deepseek.com/v1/
      - OPENAI_API_KEY=sk-131ab61ecc8c4ad4a760ab36f88ebf2f
      - MODEL=deepseek-chat
    volumes:
      - ./files:/app/files
    ports:
      - 8000:8000
      - 8080:8080
